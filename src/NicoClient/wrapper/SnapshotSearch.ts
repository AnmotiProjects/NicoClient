// import NicoClient from '../structure/NicoClient';

// class SnapshotSearch {
//     private readonly nico: NicoClient;
//     constructor(nico: NicoClient) {
//         this.nico = nico;
//     }

//     async search(query: string): Promise<SnapshotSearchResult> {
//         const url = `https://site.nicovideo.jp/api/v1/snapshot/search?q=${encodeURIComponent(
//             query,
//         )}`;
//         const response = await this.client.get(url);
//         return response.data;
//     }
// }

// export default SnapshotSearch;

// // https://site.nicovideo.jp/search-api-docs/snapshot
